## 元素
1. 模型结构：双塔
2. 线上serving过程： 召回双塔 和 粗排双塔不同。
3. 样本：负样本是关键。
4. loss：softmax loss, info nce loss
   - ctr场景下：点击是目标。所以训练目标是是否点击，是二分类问题。所以
5. 语义空间和下游任务度量空间 不一致的问题
   - 所以实际场景中，会有调节候选池比较有用的现象。
   - 电商场景下的候选池：高曝光、高点击。 短视频场景下的候选池：3s播放、7s播放、完播等多样的候选池。

## 重要点
1. 负样本的选择：（1）多样性，不同难以程度的样本。
2. 多个不同的检索池
3. 模型结构：（1）双塔模型结构优化
4. 线上索引算法的选择：线上检索算法是暴力算法的一种次优，在效率和效果上的权衡。
